<h3><%= @organisation.name %></h3>

<%= bootstrap_form_for ([@organisation, @programme]) do |f| %>
  <%= f.text_field :title, label: "New Programme Title" %><p>
  <%= f.submit %>
<% end %>

<h4>Current Programmes</h4>
  <% @organisation.programmes.each do |programme| %>
    <ul>
      <li><%= link_to programme.title, organisation_programme_path(@organisation, programme) %></li>
    </ul>
  <% end %>

<%= link_to "Edit Organisation", edit_organisation_path(@organisation) %>

<%= form_tag organisation_path(@organisation.id), method: "delete" do %>
  <%= submit_tag "Delete #{@organisation.name}" %>
<% end %>
